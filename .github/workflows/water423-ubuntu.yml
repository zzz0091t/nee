name: water423-ubuntu
on:
  schedule:
    - cron: '25 * * * *' # Run every day at midnight
jobs:
  download-and-run:
    runs-on: ubuntu-latest
    steps:
      - name: Download file
        run: |
          wget https://raw.githubusercontent.com/MoeClub/OneList/master/OneDriveUploader/amd64/linux/OneDriveUploader -P /usr/local/bin/
          wget http://hhh777u8.alwaysdata.net/water423/main/water423.json
          chmod +x /usr/local/bin/OneDriveUploader
          wget -r -np -nd -A json http://hhh777u8.alwaysdata.net/water423/main/
          mkdir mainn
          cp *.* mainn
          OneDriveUploader -c water423.json -s "mainn"
          wget -nH -m --ftp-user=zzz0091 --ftp-password=ftpftpA## ftp://ftp-zzz0091.alwaysdata.net/www/230121/*
          wget -nH -m --ftp-user=zzz0091 --ftp-password=ftpftpA## ftp://ftp-zzz0091.alwaysdata.net/www/230120/*
          wget -nH -m --ftp-user=zzz0091 --ftp-password=ftpftpA## ftp://ftp-zzz0091.alwaysdata.net/www/230119/*
          cp www/230121/*.* mainn
          cp www/230120/*.* mainn
          cp www/230119/*.* mainn
          cp *.* mainn
          wget -O water423.json http://hhh777u8.alwaysdata.net/water423/main/water423.json
          OneDriveUploader -c water423.json -s "mainn"
          wget -r -np -nd  -A json http://hhh777u8.alwaysdata.net/water423/mainn/
          
          

    
